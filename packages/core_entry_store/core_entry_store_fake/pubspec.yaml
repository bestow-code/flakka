name: core_entry_store_fake
version: 0.1.0-alpha.2
description: A store for core platform's Entry
repository: https://github.com/bestow-code/flakka

environment:
  sdk: '^3.0.5'

dependencies:
  async: ^2.11.0
  collection: ^1.17.2
  core_entry: ^0.1.1+1
  core_entry_store: ^0.1.1+2
  rxdart: ^0.27.7

dev_dependencies:
  core_entry_store_test:
  mocktail: ^0.3.0
  very_good_analysis:

index_generator:
  exclude:
    - '**.*.dart'
  indexes:
    - path: lib
      include:
        - src
      docs: Fake for Core Entry Store
