name: scaffold_test
description: Aggregate scaffold test

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.32 <0.1.0"

vars:
  package_name:
    type: string
  name:
    type: string
    description: Name of the aggregate to test
    default: Frank1
    prompt: Aggregate name
