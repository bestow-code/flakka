

import 'package:test/test.dart';

void main() {
  group('Remote Data Store', () {
    group('.initialize()', () {
      group('a new main',(){

      });
      group('a new instance',(){

      });
      group('an instance with main entry and instance entry',(){

      });
    });
    group('entries', (){});
    group('events', () {});
    group('.append', () {

    });
    group('.forward', () {

    });
  });
  test('Entry Store', () {
  });
}
