name: core_entry
version: 0.1.1

environment:
  sdk: '^3.0.5'

dependencies:
  core_common:
  freezed_annotation:
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner:
  freezed:
  json_serializable:
  test:
  very_good_analysis:

index_generator:
  exclude:
    - '**.*.dart'
  indexes:
    - path: lib
      include:
        - src
      docs: Entry model and support
