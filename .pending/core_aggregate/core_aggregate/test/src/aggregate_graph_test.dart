import 'package:test/test.dart';

void main() {
  group('.entries', () {
    // if entries includes current entry ref, emit
  });
  group('.entryRef', () {
    // if entries includes new entry ref, emit
  });
}
